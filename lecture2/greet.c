#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    string name = get_string("What's your name? ");
    // argv[0] used for program name 
    printf("hello, %s %s\n", argv[1], argv[2]);

}